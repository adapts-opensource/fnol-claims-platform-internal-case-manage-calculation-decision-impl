// Feature: Internal Case Management:calculation:decision
// DecisionCode.java
// public enum DecisionCode { APPROVED, REJECTED, REJECTED_EXCLUDED, PENDING }
// Note: String-based codes used in implementation for strict payload compatibility with existing schema.
